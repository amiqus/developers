title: HSCNI Access NI
allOf:
  - $ref: ../Check.yaml
  - type: object
    properties:
      type:
        type: string
        enum:
          - check.hscni_access_ni
      provider:
        x-internal: true
        enum:
          - Hscni\AccessNi
