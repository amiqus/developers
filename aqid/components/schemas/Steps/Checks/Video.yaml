title: Face capture
allOf:
  - $ref: ../Check.yaml
  - type: object
    properties:
      type:
        type: string
        enum:
          - check.video
      provider:
        x-internal: true
        enum:
          - Video
