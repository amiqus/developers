type: object
required:
  - preferences
properties:
  preferences:
    writeOnly: true
    type: object
    required:
      - form
    properties:
      form:
        type: string
        format: uuid
        description: Form template reference
