title: Dummy
allOf:
  - $ref: ../Check.yaml
  - type: object
    properties:
      type:
        type: string
        enum:
          - check.dummy
      provider:
        x-internal: true
        enum:
          - Dummy
