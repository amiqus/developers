title: Equifax
allOf:
  - $ref: ../Check.yaml
  - type: object
    properties:
      type:
        type: string
        enum:
          - check.credit
      provider:
        x-internal: true
        enum:
          - Equifax\Credit
